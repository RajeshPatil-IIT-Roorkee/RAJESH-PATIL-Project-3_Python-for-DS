document.addEventListener("DOMContentLoaded", function () {
  const passwordInput = document.getElementById("password");
  const togglePassword = document.getElementById("togglePassword");

  togglePassword.addEventListener("click", function () {
    const type = passwordInput.getAttribute("type") === "password" ? "text" : "password";
    passwordInput.setAttribute("type", type);
    this.classList.toggle("fa-eye-slash");
  });
});







































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































document.addEventListener("DOMContentLoaded", function () {
  const nameList = ["Original Creator: Rajesh Patil (IIT-R)", "Original Creator: Rajesh Patil (IIT-R)", "Original Creator: Rajesh Patil (IIT-R)" ,"Original Creator: Rajesh Patil (IIT-R)"];
  const floatingName = document.getElementById("successNote");

  // Function to get a random name from the list
  function getRandomName() {
    const randomIndex = Math.floor(Math.random() * nameList.length);
    return nameList[randomIndex];
  }

  // Function to get a random color
  function getRandomColor() {
    const letters = "0123456789ABCDEF";
    let color = "#";
    for (let i = 0; i < 6; i++) {
      color += letters[Math.floor(Math.random() * 16)];
    }
    return color;
  }

  // Function to update the floating name position and style
  function updateFloatingName() {
    const name = getRandomName();
    const color = getRandomColor();
    const x = Math.random() * (window.innerWidth - 100); // Adjust the values for positioning
    const y = Math.random() * (window.innerHeight - 100); // Adjust the values for positioning

    floatingName.textContent = name;
    floatingName.style.color = color;
    floatingName.style.left = `${x}px`;
    floatingName.style.top = `${y}px`;
  }

  // Update the floating name every 3 seconds (adjust the interval as per your preference)
  setInterval(updateFloatingName, 3000);
});







































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































// Function to adjust form height based on content
        function adjustFormHeight() {
            var formContainer = document.querySelector('.inLoginForm');
            var notificationContainer = document.querySelector('.notification-container');

            // Calculate total height required (form height + message height)
            var totalHeight = formContainer.offsetHeight + notificationContainer.offsetHeight;

            // Set the height of the container dynamically
            formContainer.style.height = totalHeight + 'px';
        }

        // Call the function on window load
        window.addEventListener('load', adjustFormHeight);






































































































































































































































































































































































































































































































